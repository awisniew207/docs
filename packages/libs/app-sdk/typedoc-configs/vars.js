// Configuration here is more complex than can be re-used just by using `extends` from the base config

module.exports = {
  // If you change this, make sure you update .gitignore to exclude html and assets in the new path :)
  OUT_DIR: '../docs/',
};
