{
  "jsonSchema": {
    "type": "object",
    "properties": {
      "whitelist": {
        "type": "object",
        "title": "Whitelist",
        "description": "Mapping of chain IDs to whitelisted contracts and their allowed function selectors.",
        "additionalProperties": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "functionSelectors": {
                "type": "array",
                "title": "Function Selectors",
                "items": {
                  "type": "string"
                }
              }
            },
            "required": ["functionSelectors"]
          }
        }
      }
    },
    "required": ["whitelist"]
  },
  "uiSchema": {
    "whitelist": {
      "ui:options": {
        "orderable": false
      },
      "ui:description": "Use chain IDs as keys. Each chain ID maps to a set of contract addresses.",
      "additionalProperties": {
        "ui:description": "Use contract addresses as keys. Each contract has an array of function selectors.",
        "additionalProperties": {
          "functionSelectors": {
            "ui:widget": "textarea",
            "ui:options": {
              "rows": 3
            },
            "ui:placeholder": "0xabcdef01\n0xabcdef02"
          }
        }
      }
    }
  }
}
