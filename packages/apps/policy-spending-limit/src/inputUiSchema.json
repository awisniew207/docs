{
  "jsonSchema": {
    "type": "object",
    "properties": {
      "maxDailySpendingLimitInUsdCents": {
        "type": "string",
        "title": "Max Daily Spending Limit (USD cents)",
        "description": "The maximum daily spending limit in USD cents, as a BigInt."
      }
    },
    "required": ["maxDailySpendingLimitInUsdCents"]
  },
  "uiSchema": {
    "maxDailySpendingLimitInUsdCents": {
      "ui:widget": "text",
      "ui:placeholder": "1000000",
      "ui:help": "Enter as a whole number (e.g. 1000000 = $10,000.00). Treated as a BigInt."
    }
  }
}
